# Training and test dependencies. bin/setup installs PyTorch (torch==2.14.0)
# first from the index that matches the machine (CUDA or CPU), then this file.
rlgym[rl-sim]==2.0.1
rlgym-tools==2.6.5
rlgym-learn==2.0.0
rlgym-learn-algos==0.4.2
pydantic==2.13.5
# rlgym 2.0.1 requires numpy<2.
numpy==1.26.4
wandb==0.30.0
pytest==9.1.1
# RLBot v5 runtime, imported by the parity tests. A plain `rlbot` pin
# without the version gets the old v4 package.
rlbot==2.0.0b55
rlgym-compat==2.3.6
